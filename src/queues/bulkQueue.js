// const { Queue, Worker } = require("bullmq");
// const Redis = require("ioredis");

// const connection = new Redis(process.env.REDIS_URL);

// const bulkQueue = new Queue("bulk-upload", { connection });

// module.exports = { bulkQueue, connection, Worker };
